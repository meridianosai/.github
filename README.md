# .github

`meridianosai` 组织的默认社区文件。本仓公开，组织其余仓库私有；公开只是因为 GitHub 要求默认文件必须来自公开的 `.github` 仓。

- `.github/ISSUE_TEMPLATE/` — 所有仓库默认的 Issue Form「问题 / 需求」（四段正文）。
- `scripts/sync-labels.sh` — 把通用 triage 标签创建/校正到各仓。

仓库自己有同名文件时以仓库为准。issue 的完整约定在内部文档里维护，不在本仓。
